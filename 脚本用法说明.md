# Assets/scripts 脚本用法总览

本文档整理 `./3Dunity game/Assets/scripts` 目录下当前所有 C# 脚本的用途、挂载方式、关键字段、主要方法与依赖关系，方便后续维护与场景配置。

---

## 1. minimap.cs / `Minimap`

### 作用
用于让小地图相机或其他跟随物体持续跟随目标，并保持固定俯视角度。

### 挂载方式
- 挂在小地图相机上。
- 在 Inspector 中把 `target` 指向玩家或需要跟随的对象。

### 关键字段
- `target`：要跟随的目标。
- `followSpeed`：跟随平滑速度。
- `heightOffset`：高度偏移。
- `worldOffset`：世界空间偏移。
- `fixedRotation`：固定旋转角度。

### 使用说明
- 适合用于俯视小地图。
- 若跟随目标为空，小地图不会更新位置。

### 依赖
- 无直接脚本依赖。
- `ChapterOneAutoSetup` 会尝试给它自动设置目标。

---

## 2. MissionCollectible.cs / `MissionCollectible`

### 作用
任务收集物逻辑。玩家碰到物体时，为指定任务增加进度，并可选择销毁自己。

### 挂载方式
- 挂在可收集物上。
- 物体需要有 Collider，通常应为 Trigger。

### 关键字段
- `missionManager`：任务管理器引用。
- `missionId`：要推进的任务 ID。
- `amount`：每次增加的任务进度。
- `destroyOnCollect`：收集后是否销毁。

### 使用说明
- 只会在当前任务与 `missionId` 一致时生效。
- 适合木材、武器、信件、任务道具等简单收集型任务目标。

### 依赖
- `MissionManager`

---

## 3. MissionInteractable.cs / `MissionInteractable`

### 作用
任务交互点逻辑。玩家进入范围后，可按键完成交互并推进任务，也可配置为自动触发。

### 挂载方式
- 挂在交互物体或交互区域上。
- 需要 Collider，通常设为 Trigger。

### 关键字段
- `missionManager`：任务管理器。
- `missionId`：对应任务 ID。
- `amount`：推进数值。
- `interactionKey`：交互按键，默认通常是 E。
- `requireInteractionKey`：是否必须按键才能触发。
- `triggerOnce`：是否只触发一次。
- `hideAfterInteract`：触发后是否隐藏物体。

### 使用说明
- 适合告示牌、交任务点、机关、交互 NPC 标记点等。
- 若只想玩家走进去就算完成，可关闭 `requireInteractionKey`。

### 依赖
- `MissionManager`

---

## 4. MissionTrigger.cs / `MissionTrigger`

### 作用
纯区域触发型任务逻辑。玩家进入指定区域时自动推进任务。

### 挂载方式
- 挂在区域触发器上。
- 需要 Trigger Collider。

### 关键字段
- `missionManager`：任务管理器。
- `missionId`：对应任务 ID。
- `progressAmount`：每次推进量。
- `triggerOnce`：是否只触发一次。

### 使用说明
- 适合“到达某地点”“进入某区域”“靠近某建筑”等任务。

### 依赖
- `MissionManager`

---

## 5. AnimalHealth.cs / `AnimalHealth`

### 作用
动物生命管理脚本，负责：
- 承受伤害
- 死亡处理
- 掉落肉
- 显示简易世界血条
- 禁用动物行为与延迟销毁

### 挂载方式
- 挂在动物根对象上。
- 推荐动物模型、Animator、移动脚本都挂在同一物体或其子物体结构下。

### 关键字段
- `animalName`：动物名字。
- `animalLevel`：等级。
- `maxHealth`：最大生命。
- `meatDropCount`：掉肉数量。
- `destroyDelay`：死亡后销毁延迟。
- `meatSpawnOffset`：掉落生成偏移。
- 血条相关字段：控制血条显示效果。

### 主要方法
- `TakeDamage(int damage, GameObject attacker)`：受到伤害。

### 使用说明
- 被 `PlayerSkills` 的近战攻击命中后会调用 `TakeDamage`。
- 死亡后会生成带 `MeatPickup` 的掉落物。
- 会尝试禁用 `BearMovement` 等动物行为脚本。

### 依赖
- `MeatPickup`
- `BearMovement`
- Unity `Animator`

---

## 6. BearMovement.cs / `BearMovement`

### 作用
简易熊移动逻辑，让熊自动向前移动，也支持外部控制速度与方向。

### 挂载方式
- 挂在熊对象上。

### 关键字段
- `moveSpeed`：移动速度。
- `autoMove`：是否自动移动。

### 主要方法
- `SetMoveSpeed(float)`：修改速度。
- `SetAutoMove(bool)`：开关自动移动。
- `MoveInDirection(Vector3)`：朝指定方向移动。

### 使用说明
- 当前是非常轻量的运动脚本，不是完整 AI。
- 主要用于狩猎目标的基础移动表现。

### 依赖
- 可选依赖 `Animator`
- 被 `AnimalHealth` 在死亡时禁用

---

## 7. ChapterOneAutoSetup.cs / `ChapterOneAutoSetup`

### 作用
第一章内容的自动搭建脚本。运行时自动在场景里补齐：
- 玩家相关组件
- 任务点
- 收集物
- 狩猎目标
- 任务 NPC / 交互点
- 小地图跟随目标等

### 挂载方式
- 挂在场景中的管理对象上。
- 适合用于快速初始化 demo/关卡原型。

### 关键字段
- `missionManager`：任务管理器。
- `player`：玩家对象。
- `merlinDialogue`：梅林对话对象。
- `gameplayCamera`：主相机。
- 多组位置字段：控制 NPC、武器、猎物、村民武装点等生成位置。

### 使用说明
- 适合一键配置第一章流程。
- 它会自动添加或配置多个其他脚本。
- 内部通过反射给一些私有字段赋值，因此改字段名时要特别注意同步。

### 依赖
- `MissionManager`
- `MissionTrigger`
- `MissionCollectible`
- `MissionInteractable`
- `SwordPickup`
- `NpcDialogue`
- `PlayerSkills`
- `PlayerInventory`
- `AnimalHealth`
- `BearMovement`
- `Minimap`

---

## 8. DialogueManager.cs / `DialogueManager`

### 作用
全局对话管理器，负责：
- 打开/关闭对话 UI
- 显示说话者与内容
- 按键推进对话
- 对话结束后回调 NPC

### 挂载方式
- 场景中只保留一个即可。
- 如果场景里没有，也可以通过 `EnsureInstance()` 自动创建运行时实例。

### 关键字段
- `dialogueRoot`：对话 UI 根对象。
- `speakerNameText`：说话者文本。
- `dialogueContentText`：对话内容文本。
- `continueHintText`：继续提示文本。
- `advanceKey`：推进对话按键。
- `continueHint`：提示文案。

### 主要方法
- `EnsureInstance()`：确保存在单例。
- `StartDialogue(...)`：开始对话。
- `AdvanceDialogue()`：推进到下一句。
- `EndDialogue()`：结束对话。
- `IsDialogueOpen`：当前是否正在对话。

### 使用说明
- 一般由 `NpcDialogue` 发起。
- 若未手动配置 UI，会自动创建后备 UI。

### 依赖
- `NpcDialogue`
- Unity UI

---

## 9. MeatPickup.cs / `MeatPickup`

### 作用
肉类掉落物拾取逻辑：
- 被玩家拾取
- 推进狩猎类任务
- 向玩家背包添加肉类物品

### 挂载方式
- 挂在肉掉落物上。
- 通常由 `AnimalHealth` 在死亡时动态创建并挂载。

### 关键字段
- `playerTag`：玩家标签。
- `missionId`：任务 ID。
- `missionProgressAmount`：任务推进量。
- `destroyOnPickup`：拾取后是否销毁。
- `meatLabel`：显示名称。
- `inventoryItemId`：背包里的物品 ID。

### 主要方法
- `SetMeatLabel(string)`：动态设置掉落名称。

### 使用说明
- 玩家接触后会尝试往 `PlayerInventory` 添加物品。
- 若当前任务匹配，也会给 `MissionManager` 增加进度。

### 依赖
- `MissionManager`
- `PlayerInventory`

---

## 10. ObjectiveTracker.cs / `ObjectiveTracker`

### 作用
任务追踪 UI，负责：
- 显示当前任务文本
- 在屏幕上提示当前任务目标方向
- 估算目标距离

### 挂载方式
- 挂在场景常驻对象上即可。
- 若场景里没有，`MissionManager` 会自动创建。

### 关键字段
- `objectiveTextAnchor`：文本锚点。
- `markerClampPadding`：屏幕边缘留白。
- `worldOffset`：目标显示偏移。
- `markerColor`：指示器颜色。

### 使用说明
- 会根据当前任务自动寻找目标对象。
- 内部会扫描 `NpcDialogue`、`MissionInteractable`、`MissionCollectible`、`MissionTrigger` 等脚本，并匹配它们的任务 ID。

### 依赖
- `MissionManager`
- `NpcDialogue`
- `MissionInteractable`
- `MissionCollectible`
- `MissionTrigger`

---

## 11. PlayerInventory.cs / `PlayerInventory`

### 作用
玩家背包与物品交互系统，负责：
- 武器/食物/材料分类存储
- 自动生成背包 UI
- 打开/关闭背包
- 物品拖拽换位
- 使用食物
- 从背包装备剑
- 提示文本显示

同文件还包含 `InventorySlotUI`，用于单个格子的 UI 与交互。

### 挂载方式
- 挂在玩家对象上。

### 关键字段
- `toggleInventoryKey`：打开背包按键，默认 `B`。
- `equipSwordKey`：装备武器快捷键，默认 `1`。
- `useFoodKey`：使用食物快捷键，默认 `2`。
- `weaponSlotCount` / `foodSlotCount` / `materialSlotCount`：各类槽位数量。
- `defaultFoodItemId`：默认食物 ID。
- `foodHealAmount`：食物回复量。

### 主要方法
- `AddItem(...)`：添加物品。
- `HasItem(string)`：检查是否有某物品。
- `RemoveItem(...)`：移除物品。
- `EquipSwordFromInventory()`：从背包装备剑。
- `ConsumeFood()`：消耗食物。
- 拖拽相关：`BeginDrag`、`UpdateDrag`、`EndDrag`、`HandleDrop`、`HandleSlotClick`。

### 使用说明
- `SwordPickup`、`MeatPickup` 等会调用 `AddItem`。
- 装备剑时会联动 `PlayerSkills`。
- 吃食物时会调用角色回血逻辑。

### 依赖
- `PlayerSkills`
- `SUPERCharacterAIO`
- Unity UI / EventSystem

---

## 12. PlayerVisualAttachment.cs / `PlayerVisualAttachment`

### 作用
把某个模型或视觉对象挂到当前对象下面，并统一应用本地坐标、旋转、缩放。

### 挂载方式
- 挂在玩家根对象或角色展示对象上。

### 关键字段
- `visualRoot`：要附加的视觉根对象。
- `localPosition`：本地位置。
- `localRotation`：本地旋转。
- `localScale`：本地缩放。
- `oldVisualToDisable`：旧模型，可选。

### 使用说明
- 常用于角色换皮、模型替换、把展示模型挂到控制器对象下。
- 若有旧模型，可在挂接新模型时一并隐藏。

### 依赖
- 无直接脚本依赖。

---

## 13. SwordPickup.cs / `SwordPickup`

### 作用
剑的拾取逻辑。玩家碰到后可：
- 往背包里加入剑
- 按配置决定是否立即装备
- 销毁场景中的拾取物

### 挂载方式
- 挂在场景中的剑拾取物体上。
- 物体需要 Collider，通常配合 Trigger 使用。

### 关键字段
- `playerTag`：玩家标签。
- `destroyOnPickup`：拾取后是否销毁。
- `equipImmediatelyOnPickup`：拾取后是否立即装备。

### 使用说明
- 若场景要让玩家先拾取再自己按快捷键装备，可关闭 `equipImmediatelyOnPickup`。
- 若想做成拿到即装备，可打开该项。

### 依赖
- `PlayerSkills`
- `PlayerInventory`

---

## 14. NpcDialogue.cs / `NpcDialogue`

### 作用
NPC 对话触发脚本，负责：
- 玩家靠近时显示提示
- 按键开始对话
- 对话结束后推进任务
- 根据任务进度决定是否允许对话

### 挂载方式
- 挂在 NPC 或其交互触发区对象上。
- 需要 Collider，且通常为 Trigger。

### 关键字段
#### 对话部分
- `dialogueLines`：对话内容数组。
- `interactionKey`：交互按键，默认 E。
- `oneTimeOnly`：是否只允许对话一次。

#### 提示部分
- `promptRoot`：自定义提示 UI 根对象。
- `promptText`：提示文本。
- `promptMessage`：默认提示语。

#### 任务部分
- `missionManager`：任务管理器。
- `missionId`：对话完成后要推进的任务。
- `completeMissionWhenDialogueEnds`：对话结束时是否自动加任务进度。

#### 解锁条件部分
- `gateMode`：对话开放条件。
- `gateMissionId`：用于判定的任务 ID。
- `lockedPromptMessage`：未解锁时的提示语。

### 主要方法
- `StartDialogue()`：开始对话。
- `IsUnlocked()`：判断当前是否允许对话。
- `NotifyDialogueFinished()`：由 `DialogueManager` 在结束时回调。

### 使用说明
- 很适合做主线 NPC、阶段性解锁 NPC、任务对话门槛。
- 若未配置自定义提示 UI，会自动创建运行时提示框。

### 依赖
- `DialogueManager`
- `MissionManager`
- `SUPERCharacterAIO`（用于识别玩家）

---

## 15. MissionManager.cs / `MissionManager`

### 作用
全局任务系统核心，负责：
- 保存任务列表
- 控制当前任务流程
- 增加任务进度
- 完成任务并切到下一任务
- 显示任务 UI
- 自动确保目标追踪器存在

### 挂载方式
- 场景中通常只保留一个。
- 建议挂在常驻管理对象上。

### 关键字段
#### 任务数据
- `missions`：任务列表。
- `startingMissionIndex`：起始任务索引。

#### UI
- `missionTitleText`
- `missionDescriptionText`
- `missionProgressText`

#### 调试
- `skipMissionKey`：跳过当前任务的调试按键，默认 `P`。

### 主要方法
- `StartMission(int)`：开始指定任务。
- `AdvanceToNextMission()`：切到下一任务。
- `SkipCurrentMission()`：调试跳过。
- `CompleteCurrentMission()`：完成当前任务。
- `IsMissionCompleted(string)`：判断任务是否已完成。
- `AddProgress(string, int)`：增加任务进度。
- `IsCurrentMission(string)`：判断是否是当前任务。
- `GetMissionIndex(string)`：通过 ID 查索引。
- `GetMissionByIndex(int)`：按索引取任务。
- `LoadChapterOneMissions()`：快速装载第一章任务链。

### 使用说明
- 是所有任务相关脚本的中心。
- 当前项目里 `talk_to_merlin` 任务完成后，会自动解锁 `PlayerSkills` 的幻影技能。
- 如果 Inspector 里没手动指定 UI，会自动创建一套后备任务面板。

### 依赖
- `ObjectiveTracker`
- `PlayerSkills`
- Unity UI

---

## 16. PhantomSkill.cs / `PhantomSkill`

### 作用
幻影技能实体逻辑，负责：
- 生成技能视觉体
- 延迟造成范围伤害
- 给幻影挂剑
- 播放攻击动画
- 随时间淡出并销毁

### 挂载方式
- 不需要手动挂在场景物体上。
- 通常由 `PlayerSkills.TryUseDefaultSkill()` 动态创建空物体后再 `AddComponent<PhantomSkill>()`。

### 关键字段
该脚本主要通过 `Initialize(...)` 注入参数，不依赖太多 Inspector 配置。

### 主要方法
- `Initialize(...)`：初始化伤害、范围、视觉、武器、寿命等。

### 使用说明
- 幻影技能不是拾取物，也不是常驻对象，而是运行时生成的临时攻击体。
- 若 `visualPrefab` 为空，会生成简化的替代视觉。
- 若 `phantomSwordPrefab` 不为空，会自动挂到幻影手骨上。

### 依赖
- `PlayerSkills`
- `AnimalHealth`
- `Animator`

---

## 17. PlayerSkills.cs / `PlayerSkills`

### 作用
玩家战斗与技能核心脚本，负责：
- 是否持剑
- 攻击/防御输入
- 血量/蓝量管理
- 蓝量回复
- 幻影技能释放
- 剑的视觉挂接
- 对动物造成近战伤害
- 向 UI 广播状态变化事件

### 挂载方式
- 挂在玩家对象上。

### 关键字段
#### 剑与属性
- `hasSword`
- `baseAttackPower`
- `swordAttackPower`
- `baseDefense`
- `swordDefenseBonus`

#### 输入
- `attackKey`
- `defenseKey`
- `skillKey`

#### 生存与法力
- `maxHealth`
- `startingHealth`
- `maxMana`
- `startingMana`
- `manaRegenPerSecond`

#### 动画与持剑显示
- `animator`
- `attackTriggerName`
- `defenseTriggerName`
- `equippedSwordPrefab`
- `swordHandBoneName`
- `swordHandBoneFallbackNames`
- `swordLocalPosition / Rotation / Scale`
- `swordFallbackLocalPosition / Rotation / Scale`

#### 攻击
- `attackCamera`
- `attackOrigin`
- `attackRange`
- `attackRadius`
- `attackMask`
- `attackCooldown`
- `attackDamageDelay`

#### 幻影技能
- `phantomSkillUnlocked`
- `phantomVisualPrefab`
- `phantomSwordPrefab`
- `phantomVisualLayerName`
- `phantomSkillManaCost`
- `phantomSkillCooldown`
- `phantomSkillDamageMultiplier`
- `phantomSkillRangeMultiplier`
- `phantomSkillRadiusMultiplier`
- `phantomSkillDamageDelay`
- `phantomLifetime`
- `phantomSpawnOffset`
- `lowManaHintText`
- `lowManaHintCooldown`

### 主要公开属性/事件
- `AttackPower`
- `DefensePower`
- `HasSword`
- `CurrentHealth` / `MaxHealth`
- `CurrentMana` / `MaxMana`
- `PhantomSkillUnlocked`
- `PhantomSkillCooldownRemaining`
- `PhantomSkillCooldownNormalized`
- `SwordEquipChanged`
- `HealthChanged`
- `ManaChanged`
- `PhantomSkillUnlockedChanged`
- `HintRequested`

### 主要方法
- `UnlockSword()`：解锁剑。
- `SetSwordEquipped(bool)`：设置是否装备剑。
- `Attack()`：普通攻击。
- `Defend()`：防御动作。
- `TryUseDefaultSkill()`：尝试释放幻影技能。
- `UnlockPhantomSkill()`：解锁幻影技能。
- `TakeDamage(int)`：玩家受伤。
- `RestoreHealth(int)`：回血。
- `ConsumeMana(int)`：消耗蓝量。
- `RestoreMana(int)`：回蓝。
- `DealSlashDamage(...)`：执行一次范围斩击伤害判定。

### 使用说明
- 当前普通攻击与技能都围绕“持剑”状态工作。
- `SwordPickup` 和 `PlayerInventory` 会控制它的装备状态。
- `MissionManager` 在特定任务完成后会解锁幻影技能。
- 剑的模型现在使用现成 `Prefab` 而不是代码拼模型。

### 依赖
- `PhantomSkill`
- `AnimalHealth`
- `MissionManager`

---

## 18. PlayerStatusUI.cs / `PlayerStatusUI`

### 作用
玩家状态 HUD，负责：
- 显示血条/蓝条
- 显示血量与蓝量数字
- 显示技能冷却圆环
- 显示缺蓝提示
- 对话时上移 HUD，避免遮挡

### 挂载方式
- 挂在场景中的任意对象或玩家对象上都可以。
- 场景中只应保留一个实例。

### 关键字段
- `playerSkills`：玩家技能脚本。
- `dialogueHudYOffset`：对话时 HUD 上移距离。
- `hudMoveSpeed`：HUD 平滑移动速度。

### 使用说明
- 会自动创建整套状态 UI。
- 血条与蓝条现在使用 `RectTransform` 宽度变化，不再依赖 `fillAmount`。
- 技能冷却图标仍然使用径向 `fillAmount` 表现冷却覆盖。
- 会监听 `PlayerSkills` 的生命、蓝量、提示事件。

### 依赖
- `PlayerSkills`
- `DialogueManager`
- Unity UI

---

## 总体依赖关系概览

### 玩家侧核心
- `PlayerSkills`：战斗、法力、技能核心
- `PlayerInventory`：背包、物品、装备入口
- `PlayerStatusUI`：状态显示
- `PlayerVisualAttachment`：模型挂接

### 任务侧核心
- `MissionManager`：任务总控
- `ObjectiveTracker`：任务追踪显示
- `MissionTrigger` / `MissionCollectible` / `MissionInteractable`：三类任务推进节点

### 对话侧核心
- `DialogueManager`：全局对话控制
- `NpcDialogue`：NPC 对话入口

### 战斗/掉落侧核心
- `AnimalHealth`：动物生命与死亡掉落
- `BearMovement`：动物移动
- `MeatPickup`：肉类拾取
- `SwordPickup`：剑拾取
- `PhantomSkill`：幻影技能实体

### 关卡自动配置
- `ChapterOneAutoSetup`：自动创建并串联大量系统

---

## 当前项目的典型流程

### 新手主线流程
1. `MissionManager` 装载并推进主线任务。
2. `MissionTrigger` / `NpcDialogue` / `MissionCollectible` / `MissionInteractable` 负责各阶段推进。
3. `ObjectiveTracker` 为当前任务提供方向提示。
4. 玩家通过 `SwordPickup` 获得剑，并在 `PlayerInventory` / `PlayerSkills` 中装备。
5. 玩家使用 `PlayerSkills` 攻击动物，`AnimalHealth` 掉落 `MeatPickup`。
6. 回收猎物后推进任务，继续对话。
7. 完成 `talk_to_merlin` 后，`MissionManager` 解锁 `PlayerSkills` 的幻影技能。
8. `PlayerStatusUI` 全程显示生命、法力与技能冷却。

---

如果后续你继续增加脚本，建议也按本文档的格式追加，便于统一维护。